<div class="index"
  data-controller="filter mapbox"
  data-mapbox-location-value="<%= @coordinates_hash.to_json %>"
  data-mapbox-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"
  data-action="filter->mapbox#filter"
  >
  <%= render "shared/side_search_col" %>

  <div class="map" style="width:100%; height: 100vh;"
    data-mapbox-target="mapElement"
    >
    </div>
</div>
